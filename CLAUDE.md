# Project Rules

## Forbidden Commands
- **Never use `killall`** — it is not available on Windows and can cause unintended side effects. Use targeted approaches instead (e.g. find and kill specific PIDs if needed).
